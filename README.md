# Extract_Invoice

A Streamlit application for bulk data extraction from Indian Customs Shipping Bill PDFs without utilizing external APIs.

## Setup

1. `pip install -r requirements.txt`
2. `streamlit run streamlit_app.py`
